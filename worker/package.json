{
  "name": "attention-site-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generic Cloudflare Worker. It serves one audience site from KV, uploaded by the engine.",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
